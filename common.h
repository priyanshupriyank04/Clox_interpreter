#ifndef clox_common_h   // header guard for user defined libraries to ensure only once libraries are defined
#define clox_common_h

#include <stdbool.h>   // library for importing bool datatype
#include <stddef.h>    // for null pointers and counting size safely
#include <stdint.h>     // for consistent int datatype throughout the program

#define DEBUG_TRACE_EXECUTION



#endif  // end of header guard

